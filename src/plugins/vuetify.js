import Vue from 'vue';
import Vuetify from 'vuetify/lib';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      light: {
        primary: '#C9C9C9',
        secondary: '#C5E0D8',
        accent: '#CEABB1',
      }
    }

  },
});
